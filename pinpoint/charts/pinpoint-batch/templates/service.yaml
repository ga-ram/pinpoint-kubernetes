apiVersion: v1
kind: Service
metadata:
  name: {{ include "pinpoint-batch.fullname" . }}
  labels:
    {{- include "pinpoint-batch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pinpoint-batch.selectorLabels" . | nindent 4 }}
